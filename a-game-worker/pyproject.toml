[project]
name = "a-game-worker"
description = "A-Game injection worker for sports data"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "aio-pika>=10.0",
    "asyncpg>=0.30",
    "httpx>=0.28",
    "pydantic-settings>=2.7",
]
